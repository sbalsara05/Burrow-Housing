apiVersion: v1
kind: Service
metadata:
  name: burrow-service
spec:
  type: NodePort
  selector:
    app: burrow
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30007  # Minikube will expose this port
